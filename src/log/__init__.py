"""Logging utilities.

Provides the `CustomLogger` with helpers for one-line aggregation and
level-based filtering. Import the class explicitly:

    from log import CustomLogger
"""

from .logger import CustomLogger

__all__ = ["CustomLogger"]
